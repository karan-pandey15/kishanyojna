# Public site origin for canonical URLs, Open Graph, and sitemap.
# Safe to expose — never put secrets, database passwords, or service_role keys here.
NEXT_PUBLIC_SITE_URL=https://www.bhartiyakisanunionumakanth.org

# ---------------------------------------------------------------------------
# Supabase (anon / publishable key only — NEVER put service_role in NEXT_PUBLIC_*)
# Copy to `.env.local` and fill from: Supabase → Project Settings → API
# ---------------------------------------------------------------------------

NEXT_PUBLIC_SUPABASE_URL=https://xbilseihfpvijzdlisie.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_u4XtFBdqqqltccAToTHLaw_Z4fRr6CJ
# Optional: Cloudflare Turnstile site key (public). Secret key must stay server-only.
# NEXT_PUBLIC_TURNSTILE_SITE_KEY=
